name: Stale PRs

on:
  schedule:
    # Everyday at 2am
    - cron: '0 2 * * *'

jobs:
  stale_prs:
    runs-on: ubuntu-latest
    steps:
      - run: curl -d "coq:coq" https://coqbot.herokuapp.com/check-stale-pr
